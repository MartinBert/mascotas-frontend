import LoggedUserContextProvider from './LoggedUserContextProvider'
import PrivateRouteContextProvider from './PrivateRouteContextProvider'
import ProductSelectionModalContextProvider from './ProductSelectionModalContextProvider'
import SaleContextProvider from './SaleContextProvider'

const contextProviders = {
    LoggedUserContextProvider,
    PrivateRouteContextProvider,
    ProductSelectionModalContextProvider,
    SaleContextProvider
}

export default contextProviders